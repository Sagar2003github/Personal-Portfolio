CREATE database details;
use details;

CREATE TABLE messages (
  id INT AUTO_INCREMENT PRIMARY KEY,
Name VARCHAR(255),
  email VARCHAR(255),
message VARCHAR(255) 
);



SELECT * FROM messages;